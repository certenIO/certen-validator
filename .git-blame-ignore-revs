# Commits that only reformat code. `git blame` skips these so it reports the
# author of the logic, not of the formatting sweep.
#
# Enable locally (once per clone):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it changes NOTHING but formatting. Verify with:
#   git show <rev> --stat   and confirm every file is byte-identical to
#   gofmt of its previous content.

# Normalize the main module to gofmt (122 files, no logic change)
b51868a201ad87d67b6898809766c18911bf4faa
